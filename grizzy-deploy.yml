http:
  routers:
    grizzy-deploy-router:
      rule: "Host(`deploy.grizzy-deploy.com`)"
      service: grizzy-deploy
      priority: 2

  services:
    grizzy-deploy:
      loadBalancer:
        servers:
          - url: "http://localhost:3002/"